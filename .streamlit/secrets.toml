# Fichier présent pour éviter l'avertissement Streamlit « No secrets files found » en local.
# Les clés réelles peuvent rester dans lexmaroc/.env (chargé par app.py via python-dotenv).
# Sur Streamlit Cloud, utilisez l'écran Secrets du tableau de bord à la place.

LOCAL_USE_DOTENV = "1"
